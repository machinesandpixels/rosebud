import React from 'react';
import footer from '../../styles/Footer.module.css';

const Footer = () => {
  return (
    <footer className={footer.container}>
      <div>
        <h1 className={footer.title}>ROSEBUD IMAGES</h1>
      </div>
    </footer>
  )
}

export default Footer;